"use client";import {useState} from 'react';import {projects, type Project} from '@/data/projects';import {motion,AnimatePresence} from 'framer-motion';import Link from 'next/link';export default function HoverPreviewList(){const [active,setActive]=useState<Project>(projects[0]);return(<section id="projects" className="grid md:grid-cols-2 gap-8 items-start"><ul className="divide-y divide-zinc-800">{projects.map(p=>(<li key={p.slug} className="py-5 cursor-pointer group" onMouseEnter={()=>setActive(p)} onFocus={()=>setActive(p)}><div className="flex items-baseline gap-3"><span className="text-[10px] font-mono tracking-wider text-zinc-500">{p.year}</span><Link href={`/projects/${p.slug}`} className="block text-base md:text-lg font-medium group-hover:text-white text-zinc-200 transition-colors">{p.title}</Link></div><p className="mt-1 text-sm text-zinc-400 max-w-2xl">{p.summary}</p><div className="md:hidden mt-3 overflow-hidden rounded-xl border border-zinc-800"><img src={p.image} alt="Project preview" className="w-full h-56 object-cover"/></div></li>))}</ul><div className="hidden md:block sticky top-10"><div className="aspect-[4/3] w-full overflow-hidden rounded-2xl border border-zinc-800 bg-zinc-900"><AnimatePresence mode="wait"><motion.img key={active?.slug} initial={{opacity:0,scale:1.02}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:0.98}} transition={{duration:0.35,ease:'easeOut'}} src={active?.image} alt="Preview" className="w-full h-full object-cover"/></AnimatePresence></div><div className="mt-4 text-xs text-zinc-400"><div className="uppercase tracking-widest text-[10px] text-zinc-500">Role</div><div>{active?.role}</div></div></div></section>) }
