import {projects} from '@/data/projects';export default function ProjectPage({params}:{params:{slug:string}}){const project=projects.find(p=>p.slug===params.slug);if(!project)return <div className='py-20'>Project not found.</div>;return(<main className='space-y-6'><div className='aspect-[16/7] w-full overflow-hidden rounded-2xl border border-zinc-800 bg-zinc-900'><img src={project.image} alt={project.title} className='w-full h-full object-cover'/></div><div className='space-y-2'><h1 className='text-2xl md:text-4xl font-semibold'>{project.title}</h1><div className='text-xs uppercase tracking-widest text-zinc-500'>{project.year} · {project.role}</div><p className='text-zinc-300'>{project.summary}</p></div><article className='prose prose-invert prose-zinc max-w-none'><p>{project.content}</p></article></main>) }
